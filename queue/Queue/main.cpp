#include "Sequentialqueue.h"
#include "Sequentialqueue.cpp"
#include "LinkedQueue.h"
#include "LinkedQueue.cpp"

int main() {
//    Sequentialqueue<int> queue(10);
//    int g;
//    for(int i = 0; i < 10; ++i)
//        queue.enQueue(i);
//    queue.deQueue(g);
//    queue.enQueue(233);
//    queue.enQueue(666);
//    queue.showQueue();
//    LinkedQueue<int> lQueue;
//    for(int i = 0; i < 10; ++i)
//        lQueue.enQueue(i);
//    lQueue.showQueue();
    return 0;
}