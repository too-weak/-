#include <iostream>
#include <vector>
#include <queue>
using namespace std;

int main() {
    vector<queue<int>> vec;
    for(int i = 0; i < 10; ++i){
        vec.push_back(queue<int>());
    }
    vec[3].pop()
    return 0;
}