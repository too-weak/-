#include "Car.h"

void Car::move(){
    ++movedTimes;
}

string Car::leave(){
    string message;
    message = plate + " " + "was moved " + to_string(movedTimes)
         + " times while it was here" + '\n';
    return message;
}

string Car::getPlate(){
    return plate;
}


